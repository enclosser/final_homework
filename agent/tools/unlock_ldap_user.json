{
  "name": "unlock_ldap_user",
  "description": "Разблокировать учётную запись LDAP. Чувствительная операция: требует подтверждения.",
  "sensitive": true,
  "parameters": {
    "type": "object",
    "properties": {
      "email": {
        "type": "string"
      }
    },
    "required": ["email"]
  }
}