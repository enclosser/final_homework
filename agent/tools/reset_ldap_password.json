{
  "name": "reset_ldap_password",
  "description": "Сбросить пароль пользователя LDAP. Чувствительная операция: требует подтверждения.",
  "sensitive": true,
  "parameters": {
    "type": "object",
    "properties": {
      "email": {
        "type": "string"
      }
    },
    "required": ["email"]
  }
}